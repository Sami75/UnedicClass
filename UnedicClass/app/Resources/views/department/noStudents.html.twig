{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="panel panel-default panelDesign">
                    <div class="panel-heading"><h3>{{ department.name }} - 0/{{ department.capacity }}</h3></div>

                    <div class="panel-body text-center">
                        <p class="lead">Aucun élève n'est pour le moment inscrit en {{ department.name }}.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
