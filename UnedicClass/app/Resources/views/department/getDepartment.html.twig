{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="panel panel-default">
                    <div class="panel-heading"><h3>{{ students[0].getDepartment().name }} - {{ students|length }}/{{ students[0].getDepartment().capacity }} élèves.</h3></div>

                    <div class="panel-body panelDesign">

                        {% for student in students %}
                            <div class="row">
                                <p class="lead">
                                    <a href="{{ path('getStudent', { 'numetud': student.id }) }}">{{ student.firstname }} {{ student.lastname }}</a>
                                </p>
                            </div><hr>
                        {% endfor %}  
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
